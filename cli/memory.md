## Memory

* cli instructions for memory management

```
/init               Creates a new CLAUDE.md file in your project
/memory             edit memory files
# remember this     add on line to memory file
@path/to/file

```

* load CLAUDE.md files from additional directories beyond the current working directory.

```
claude --add-dir /path/to/other/project
```


