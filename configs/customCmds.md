# custom commands

adding new cmds:

```
.claude
    commands
        newCmd.md
```
* after claude restart it will add /newCmd

## params

```

* to have params here use the @ARGUMENTS

* use @/ prefix to 
```
